# Ubuntu Helper Notes

Ubuntu or Debian helpers are a practical default for public setups.

They work well for:

- remote pwn probing
- source-build lanes
- trace collection
- reverse fallback tasks

Use `remote-probe` to verify the helper before relying on it in a solve flow.
